@model (string Title, string Value, string Subtitle)

<div class="summary-card">
  <div class="summary-card-title">@Model.Title</div>
  <div class="summary-card-value">@Model.Value</div>
  <div class="summary-card-subtitle">@Model.Subtitle</div>
</div>